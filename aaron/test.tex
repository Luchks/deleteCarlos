\documentclass[12pt,a4paper]{article}
\usepackage[spanish]{babel}
\usepackage{fontspec}
\usepackage{tikz}
\usetikzlibrary{decorations.text}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{datatool}

\geometry{top=2cm, bottom=2cm, left=2cm, right=2cm}

% Cargamos el archivo list.csv
\DTLloaddb{lista}{list.csv}

\begin{document}

% Iteramos usando las minúsculas exactas del archivo list.csv
\DTLforeach{lista}{%
    \Materia=curso, \Profesor=docente%
}{%
    \begin{titlepage}
        \centering
        \begin{tikzpicture}[remember picture, overlay]
            
            % Nombre en ARCO
            \path [decorate, decoration={text along path, 
                text={|\Huge\bfseries|AARON ADRIANO LOYOLA LUIS},
                text align=center}] 
                ([xshift=-8cm, yshift=-6cm]current page.north) 
                arc [start angle=150, end angle=30, radius=9cm];


            % Mascota
            \node at (current page.center) {
                \includegraphics[width=0.55\textwidth]{mascota.png}
            };

            % CURSO (Usando \Materia vinculado a 'curso')
            \node[font=\Huge\bfseries] at ([yshift=5cm]current page.south) {\Materia};
            
            
        \end{tikzpicture}
    \end{titlepage}
    \newpage
}

\end{document}
